<!-- SPDX-License-Identifier: MPL-2.0 -->

# Loop 27 — portable state and complete Wails removal

Status: WINDOWS_PASS. Windows M3 criteria passed at production/workflow revision `e0ea18a`; local HEAD `661dd8a` changes only this evidence document. Linux/macOS native acceptance remains deferred to their later platform phases, not waived. The owner clarified that the complete Windows product, including later feature work, comes before Linux development, and Linux before macOS. Loops 24–26 passed; Loop 26 exact-SHA CI `35974229139` and owner-visible Windows Home-to-MPV check passed. Preserve all pre-existing dirty files and historical Loop 23/RA evidence.

| Field | Contract |
| --- | --- |
| Scope | Resolve Windows data from the executable folder; use `<root>/data/animeportable.db`; offer an explicit one-time copy of the old config profile before first portable open; keep user-installed MPV external; build and accept the Windows extracted-folder artifact; remove active and packaged Wails/Svelte/NSIS/WebView paths and obsolete tooling/dependencies. Existing Linux/macOS paths and artifacts remain provisional pending their later platform phases. |
| Success | Fresh folder, repeat launch, upgrade in place and moving the whole folder retain local state without writing to OS profile; existing legacy data is never silently imported, changed or deleted; import captures committed WAL state, validates integrity/schema, installs atomically without overwriting an existing target and handles concurrent attempts; read-only, linked, corrupt, interrupted and non-ASCII paths fail safely; external MPV discovery/config remains valid; artifacts contain native Fyne app and no WebView/installer/frontend payload. |
| Deterministic checks | Focused portable-path/import concurrency/security tests, backend startup/UI choice tests, existing full Go regression/race/vet, module and vulnerability checks, Windows portable build/extract/source/dependency scans. Keep M2 Windows live MPV and native tests. Linux/macOS runner results remain diagnostics for later phases; never call a cross-build native runtime PASS. |
| Human/platform gates | Owner sees first-run choice and old-data copy result on Windows without internal IDs/files; extracted Windows artifact runs without installer or privileged registration. Windows native keyboard/mouse and runtime limits are checked; Windows M4 integrated interaction/resource gates stay open. Linux/macOS human gates move to their later platform phases. |
| Reviews | Fresh code-quality, security/persistence, platform/package and workflow verifier/security reviews; `.github/workflows/**` change stays inside the previously approved scope and receives independent review before push. |
| Exclusions | No Search/detail/etc. feature screens in this loop; those resume on Windows after Windows Loop 28 PASS. No main push/PR/release in this loop. |

Windows M3 PASS requires the Windows M3 criteria above and the existing review gates; Windows Loop 28 separately requires final-state regression, MPV, keyboard/mouse, portable package, no-WebView and the unchanged Windows resource envelope. Existing Windows owner-visible import/playback checks count as evidence at their recorded source revision; repeat only when a relevant change invalidates them. After Windows Loop 28 PASS, a Windows-only `main` merge may be proposed with explicit remote authorization, and Loop 29 may begin on Windows. Linux follows after the full Windows product is stable; macOS follows after Linux is stable. Neither later platform is supported until its own gates pass. The owner authorized Windows long-idle resource measurement on 2026-09-24.

M0's detailed portable state and import contract in `docs/LOOP24_M0_CONTRACT.md` is normative. The import is an opt-in copy, not a move. Its source is `os.UserConfigDir()/AnimePortable/animeportable.db`; the source and its WAL/SHM are never mutated. A consistent SQLite snapshot is staged under the portable data directory and validated before an exclusive no-clobber install. A mere existence check followed by normal rename is insufficient. The default first-run action creates a new empty portable profile. The UI must not ask the owner for IDs, database edits, paths or command-line operations.

Fyne v2.8.1 itself creates cache/config directories in the OS profile and exposes no supported path override. This conflicts with the portable state contract. On 2026-09-24 the owner authorized process-local environment redirection for Fyne to `<root>/data`, provided external MPV discovery and its child process retain the original user environment and all three OS paths are verified. The redirect must not change machine or persistent user settings. A fatal startup error may use a removed-on-exit temporary UI cache solely to display the error; it may not create an alternate profile or open data there. Linux/macOS CI process liveness is not a substitute for visible native operation and owner/platform gates.

Checkpoint `c7ce2f1` was committed on the authorized temporary branch and subsequently pushed. On Windows, `go test -count=1 ./...`, `go test -race -count=1 ./...`, `go vet ./...`, `go mod verify`, 20 repetitions of native Home keyboard/cancellation, production build and ZIP extraction passed. The ZIP contained only the native executable, `LICENSE` and `THIRD_PARTY_NOTICES.md`. An isolated visible Windows run from the extracted ZIP showed the explicit new/copy choices and focused default; the owner reported that copying the fixture displayed `Portable Import Demo` and keyboard/mouse operation passed. The fixture source SHA-256 remained `51B486DD68DA43712C7FB08053CEE26DE016B4F38414FEC2252E83EF1827844D`; a portable database appeared under extracted `data/`. Fyne settings/cache appeared under that `data/`; no Fyne directory appeared under the isolated original user profile/cache. The test app exited after a normal close message. This is Windows human/host evidence; hosted CI evidence is recorded below.

The Linux package targets a desktop with a compatible OpenGL driver and the system X11 graphics libraries; the current CI build uses Mesa, Xorg, Wayland and xkbcommon development packages, while runtime needs depend on the target distribution's graphics stack. The macOS bundle declares macOS 12.0 as its minimum and uses macOS-provided graphics frameworks. Neither archive installs system libraries or registers an application. These are runtime boundaries, not claims that every Linux distribution or older macOS release can launch it. [Fyne's platform prerequisites](https://docs.fyne.io/started/quick/) distinguish build dependencies from end-user setup; native runner evidence must still confirm the extracted artifacts actually start.

The first hosted M3 run [`36053351445`](https://github.com/loustack17/AnimePortable/actions/runs/36053351445) at `58fdaf57` failed on three distinct criteria. macOS test fixtures used Go's `/var` temporary path, which traverses the system `/var` symlink and was correctly rejected by the portable path rule; the native test step now resolves only `TMPDIR` to its physical path, retaining explicit linked-path rejection tests. Linux's read-only import test detected source SQLite artifact mutation when opening an existing WAL and SHM; WAL imports now copy the source database and WAL into private staging before SQLite opens it. Windows' first extracted launch did not create Fyne's app cache subtree because the portable cache root did not yet exist; the redirect now creates that root before Fyne starts and validates all Fyne state paths. The path validator also rejects Windows junctions, which Go reports as irregular files. A fresh isolated Windows host diagnostic after the cache-root fix found the app cache and preferences under `data/`, no Fyne path under the original profile/cache, and a live app after ten seconds; this host run is diagnostic only.

The second hosted run [`36079332274`](https://github.com/loustack17/AnimePortable/actions/runs/36079332274) at `8644e86` passed [Windows native/build/extracted launch/live MPV](https://github.com/loustack17/AnimePortable/actions/runs/36079332274/job/107897517476) and [Linux full regression/race/vet/vulnerability/build/extracted visible-window smoke](https://github.com/loustack17/AnimePortable/actions/runs/36079332274/job/107897517773). macOS native, SQLite and package tests ran; three backend fixtures failed because they placed the executable outside the `.app/Contents/MacOS` layout required by the production macOS plan. Those fixtures now use the real bundle layout without changing assertions, independently reviewed and locally committed at `a13d92a`.

The third hosted run [`36080003315`](https://github.com/loustack17/AnimePortable/actions/runs/36080003315) at `844f4c3` passed [Windows](https://github.com/loustack17/AnimePortable/actions/runs/36080003315/job/107899685763) and [Linux](https://github.com/loustack17/AnimePortable/actions/runs/36080003315/job/107899685453) again. [macOS](https://github.com/loustack17/AnimePortable/actions/runs/36080003315/job/107899685752) passed native/SQLite/package tests and built/extracted the `.app`, then panicked while opening the real GLFW window: `NSGL: Failed to find a suitable pixel format`. This matches [Fyne's macOS virtual-machine issue](https://github.com/fyne-io/fyne/issues/2697); GitHub [documents standard hosted runners as virtual machines](https://docs.github.com/en/actions/reference/runners/github-hosted-runners). Classify this as a macOS hosted-runner graphics capability limit, not a native macOS runtime PASS. The macOS archive upload was moved before the unchanged smoke step so a capable physical Mac can test the artifact even while CI remains red. The owner deferred the Linux visible check and has no macOS machine; both human/platform gates remain open. M3 remained open at this checkpoint; the later Windows-scoped reconciliation is recorded below.

The final code/workflow run [`36080446540`](https://github.com/loustack17/AnimePortable/actions/runs/36080446540) at `e0ea18a` passed [Linux](https://github.com/loustack17/AnimePortable/actions/runs/36080446540/job/107901003536) and [Windows](https://github.com/loustack17/AnimePortable/actions/runs/36080446540/job/107901003874). [macOS](https://github.com/loustack17/AnimePortable/actions/runs/36080446540/job/107901003724) uploaded artifact `animeportable-darwin-portable` (ID `10842165390`) after native tests and package/extract, then failed the unchanged real-window smoke with the same NSGL error. The downloaded inner `animeportable-darwin-arm64.zip` SHA-256 is `E37DB543DFDF064EFDC08672301D8AACCCC24615D573DE1B8C9DFF23F8B9B320`; it contains only the `.app` executable, icon, plist, license and notices. Artifact availability proves packaging, not runtime operation. The Linux artifact is available, but the owner replied “not now” to its visible check. Under the revised Windows-first contract, these later-platform gaps do not block Windows M3, but remain open for their later phases.

Windows M3 final reconciliation: the Windows job passed native/portable build and extraction, isolated startup with Fyne state under portable `data/`, and real external MPV checks at the exact production/workflow SHA `e0ea18a`. The owner accepted the extracted Windows first-run copy UI, visible focus, imported demo content and keyboard/mouse operation; the source database hash remained unchanged. The source and tracked package scan found no active Wails/Svelte/NSIS/WebView/frontend files or dependencies; the archive contains exactly `animeportable.exe`, `LICENSE` and `THIRD_PARTY_NOTICES.md`. An independent read-only final Windows code/security/package review at `661dd8a` found no MUST_FIX; that revision changed only this evidence document after the tested production SHA. The repository reports no protected-branch required status checks; the Windows job is green, while the aggregate workflow remains red solely because the deferred macOS graphical smoke cannot initialize NSGL on the hosted VM. This is **WINDOWS_PASS for Loop 27**, not a claim that Linux/macOS passed or that Loop 28's resource and final integration gates passed.
